package com.roninsoulkh.mappingop.ui.theme

import androidx.compose.ui.graphics.Color

// === MappingOP Design System (2025) ===

// --- DARK PALETTE (Deep Enterprise) ---
val DarkBackground = Color(0xFF0F172A) // Глубокий темно-синий
val DarkSurface = Color(0xFF1E293B)    // Картки чуть светлее
val DarkTextPrimary = Color(0xFFFFFFFF)
val DarkTextSecondary = Color(0xFF94A3B8) // Slate Grey

// --- LIGHT PALETTE (Clean Corporate) ---
val LightBackground = Color(0xFFF1F5F9) // Светло-серо-голубой (Slate 100)
val LightSurface = Color(0xFFFFFFFF)    // Чисто белые карточки
val LightTextPrimary = Color(0xFF0F172A) // Текст цвета темного фона
val LightTextSecondary = Color(0xFF64748B) // Slate 500

// --- SHARED ACCENTS (Brand Identity) ---
// Эти цвета одинаковые и днем, и ночью
val CyanAction = Color(0xFF06B6D4) // Начало градиента
val BlueAction = Color(0xFF3B82F6) // Конец градиента

val StatusRed = Color(0xFFEF4444)   // Долг / Ошибка
val StatusGreen = Color(0xFF10B981) // Успех

val DividerDark = Color(0xFF334155)
val DividerLight = Color(0xFFE2E8F0)